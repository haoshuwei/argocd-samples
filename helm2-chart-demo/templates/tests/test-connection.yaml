apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm2-chart-demo.fullname" . }}-test-connection"
  labels:
{{ include "helm2-chart-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helm2-chart-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
